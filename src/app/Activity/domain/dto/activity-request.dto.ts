export class ActivityRequestDto {
  constructor(public name: string, public description: string, public location: string, public duration: number, public price: number, public picture: string) {
  }
}
